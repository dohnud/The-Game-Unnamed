/// @description state_change(state);
/// @function state_change
/// @param state
function state_change(argument0) {

	//change player to desired state when script is called
	currentState = argument0;
	frame_reset();
}
